// Consumes destructured exports
import { extractedA, renamedB, first, second, rest, deepValue, withDefault } from './destructuring';

console.log(extractedA, renamedB);
console.log(first, second, rest);
console.log(deepValue);
console.log(withDefault);
